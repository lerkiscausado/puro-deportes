/**
 * Enum de estados posibles de una inscripción.
 *
 * - ACTIVO: La inscripción está activa y participando en el torneo.
 * - ELIMINADO: La inscripción ha sido eliminada o dada de baja.
 */
export enum EstadoInscripcion {
  ACTIVO = 'Activo',
  ELIMINADO = 'Eliminado',
}
